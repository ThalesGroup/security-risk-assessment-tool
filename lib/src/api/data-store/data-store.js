const dataStore = () => {
  console.log('store json file');
};

module.exports = dataStore;

// const getValueProperties = (map) => {
//   map.forEach((value, key) => {
//     map.set(key, value.properties);
//   });
//   return map.values();
// };
